#include<stdio.h>
int main()
{
	int i,j,m,n,a[100],t[100];
	scanf("%d", &n);
	for(i=0;i<n;i++)
	{
		scanf("%d", &a[i]);
	}
	
		if(n==1)
		{
			printf("%d", a[0]);
		}
		else
		{	
			m=(a[i]+a[n-1])/2;
			
			
}
