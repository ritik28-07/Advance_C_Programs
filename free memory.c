/*  free function is used to release memory space when not required.
    the syntax of pre function is:
    free(p)
    
    memory is deallocated and returned back to free list in free heap area.
*/
